name: second_workflow
on:
    push:
        branches:
            - main
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: first step
              run: echo "this is first step"
            - name: print goodbye
              run: |
                echo "this is end of my first workflow"
                echo "goodbye"